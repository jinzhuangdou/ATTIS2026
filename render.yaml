services:
  - type: web
    name: attis-2026-review-panel
    env: python
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn shared_review_portal:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
